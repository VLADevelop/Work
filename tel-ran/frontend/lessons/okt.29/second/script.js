function sum(a, b) {
    return a + b
}

sum(10, 15)

function fn(anotherFn) {
    console.log("Hello")
    anotherFn
}

fn(sum())

function greeting(name) {
 return `Hello, $(name)`   
}

function sum(a, b) {
    return a + b
}

function min(a, b) {
    return a - b
}

lib(min, 20, 10)

// Создать функцию add, 

function add(num) {
    return num + 2 
}

// console.log(add(10)) 

let nums = [1, 2, 3, 4, 5]
let addThree = num => num.map(addThree)
console.log(newNums)

addThree(10)
let myNum = 10

